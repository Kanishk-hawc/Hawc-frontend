import {
  formatName,
  shorten
} from "./chunk-G3PIBVWD.js";
import {
  h
} from "./chunk-55A3BFCU.js";

// node_modules/@dytesdk/ui-kit/dist/esm/ChatHead-bc3b40ed.js
var differenceInMinutes = (oldDate, newDate) => {
  const diff = newDate.getTime() - oldDate.getTime();
  return Math.round(Math.abs(diff / 1e3 / 60));
};
var elapsedDuration = (oldDate, newDate) => {
  const minutes = differenceInMinutes(oldDate, newDate);
  if (minutes < 2) {
    return "just now";
  }
  if (minutes < 60) {
    return `${minutes}m ago`;
  }
  const hours = Math.round(minutes / 60);
  if (minutes < 90) {
    return `about ${hours}h ago`;
  }
  if (hours < 24) {
    return `${hours}h ago`;
  }
  const days = Math.round(hours / 24);
  if (days < 7) {
    return `${days}d ago`;
  }
  const weeks = Math.round(days / 7);
  return `${weeks}w ago`;
};
var formatDateTime = (date) => {
  return date.toDateString() + " " + date.toLocaleTimeString();
};
var ChatHead = ({ name, time, now }) => {
  return h(
    "div",
    { class: "head" },
    h("div", { class: "name" }, shorten(formatName(name), 20)),
    h("div", { class: "time", title: formatDateTime(time) }, elapsedDuration(time, now))
  );
};

export {
  differenceInMinutes,
  elapsedDuration,
  formatDateTime,
  ChatHead
};
//# sourceMappingURL=chunk-WQQOISZ6.js.map
