// node_modules/@dytesdk/ui-kit/dist/esm/string-ed1380fb.js
var shorten = (str, maxLength = 20) => {
  if (str == null)
    return "";
  if (str.length > maxLength) {
    return `${str.substring(0, maxLength)}...`;
  }
  return str;
};
var hasOnlyEmojis = (str) => {
  const num = /^\d+$/;
  const re = new RegExp("^\\p{Emoji}+$", "u");
  return re.test(str) && !num.test(str);
};
var sanitizeLink = (link) => {
  if (link === null || link === void 0 ? void 0 : link.trim().toLowerCase().startsWith("javascript:")) {
    return "https://dyte.io";
  }
  return link;
};
var formatName = (name) => {
  name = name === null || name === void 0 ? void 0 : name.trim();
  if (name === "")
    return "Participant";
  return name;
};
var whiteSpace = new RegExp(/[^\u00BF-\u1FFF\u2C00-\uD7FF\w\s]/, "g");
var space = new RegExp(/\s+/);
function getInitials(name, maxInitials = 2) {
  const cleanedName = name.replace(whiteSpace, "");
  const words = cleanedName.trim().split(space).slice(0, maxInitials);
  return words.map((word) => word.charAt(0)).join("").toUpperCase();
}

export {
  shorten,
  hasOnlyEmojis,
  sanitizeLink,
  formatName,
  getInitials
};
//# sourceMappingURL=chunk-G3PIBVWD.js.map
